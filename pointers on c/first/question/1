1、使代码更容易被阅读和修改

2、这些声明被称之为函数原型。他们告诉编译器这些以后将在源文件中定义的函数的特征。
  当这些函数被调用的时候，编译器就能对它们进行转缺陷检查。每个原型以一个类型名开头
  表示函数返回值的类型。跟在返回类型名后面的是函数的名字，再后面是函数期望接受的参数。

3、方便修改

6、一方面是为了运行效率。
另外最主要，C语言的特征就是直接操作地址，自由度高，指针就是为此存在，检查越界会限制程序员的自由度。所以自由是要付出代价的。